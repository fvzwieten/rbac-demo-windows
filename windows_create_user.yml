---
- name: create user
  hosts: role_dcserver
  vars_files:
    files/winrm_vars

  tasks:
  - name: create a domain user
    win_domain_user:
      name: "{{ username }}"
      upn: "{{ username }}@{{ domain }}"
      password: "{{ password }}"
      groups:
      - "{{ group }}"
      
  - name: export user data
    set_stats:
      data:
        ad_username: "{{ username }}"
        ad_password: "{{ password }}"
